const state = {
  username: '',
  userId: 0,
  userKey: '',
  isLogin:false
  }
const getters = {
  getTotalPrice(state){
    return state.totalPrice
  }
}
const actions = {}
const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
